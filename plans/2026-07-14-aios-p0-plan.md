# AIOS P0: Foundation & Project Management ? Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the AIOS backend skeleton with PostgreSQL + pgvector, project CRUD with archive/restore, Agent/LLM/Voice provider abstractions, a minimal React admin UI, and an Expo mobile shell.

**Architecture:** Python 3.12 FastAPI backend with SQLAlchemy 2.0 async, Pydantic v2 schemas, Alembic migrations. PostgreSQL + pgvector via Docker Compose. Abstract provider interfaces with stub agent and OpenAI LLM provider. React 18 + TypeScript + Vite admin frontend. React Native Expo shell.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy 2.0 (async), Pydantic v2, Alembic, PostgreSQL 16 + pgvector, Docker Compose, pytest + pytest-asyncio + httpx, React 18 + TypeScript + Vite, Expo (React Native)

**Derived from:** docs/specs/2026-07-14-aios-p0-foundation-design.md v1.1

---

## Task Overview

| # | Task | Files Created |
|---|------|--------------|
| 1 | Docker Compose | docker-compose.yml |
| 2 | Backend Scaffold | main.py, config.py, database.py, errors.py, requirements.txt, pytest.ini |
| 3 | Project Model + Migration | models/project.py, alembic/ |
| 4 | Project Schemas | schemas/project.py |
| 5 | Health Endpoint + Test | api/health.py, tests/api/test_health.py, conftest.py |
| 6 | Project CRUD API | api/projects.py, tests/api/test_projects.py |
| 7 | Archive/Restore API | Extend api/projects.py + tests |
| 8 | Agent + Change Model + Registry | agent/base.py, models.py, registry.py, plugins/codex_stub/ |
| 9 | Agent List Endpoint | api/agents.py, tests/api/test_agents.py |
| 10 | LLM Abstractions + OpenAI | llm/base.py, openai_provider.py, tests/ |
| 11 | Voice Abstractions | voice/base.py |
| 12 | Frontend Scaffold | Vite + React + TypeScript + API client |
| 13 | Frontend Projects Page | pages/Projects.tsx, App.tsx |
| 14 | Mobile Shell | Expo App.tsx |
| 15 | Integration Test | Full test run + curl verification |

---



### Task 1: Docker Compose ? PostgreSQL + pgvector

**Files:** Create `F:\AIOS\docker-compose.yml`

- [ ] **Step 1: Create docker-compose.yml**

```yaml
version: "3.9"
services:
  db:
    image: pgvector/pgvector:pg16
    container_name: aios-db
    environment:
      POSTGRES_USER: aios
      POSTGRES_PASSWORD: aios_dev
      POSTGRES_DB: aios
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  db_test:
    image: pgvector/pgvector:pg16
    container_name: aios-db-test
    environment:
      POSTGRES_USER: aios_test
      POSTGRES_PASSWORD: aios_test
      POSTGRES_DB: aios_test
    ports:
      - "5433:5432"

volumes:
  pgdata:
```

- [ ] **Step 2: Start the dev database**

Run: `docker compose up -d db`
Expected: Container `aios-db` starts, port 5432 listening.

- [ ] **Step 3: Verify connection**

Run: `docker compose exec db psql -U aios -d aios -c "SELECT 1"`
Expected: Output shows `1`.

---

### Task 2: Backend Scaffold

**Files:**
- Create: `F:\AIOS\backend\requirements.txt`
- Create: `F:\AIOS\backend\pytest.ini`
- Create: `F:\AIOS\backend\app\__init__.py`
- Create: `F:\AIOS\backend\app\main.py`
- Create: `F:\AIOS\backend\app\config.py`
- Create: `F:\AIOS\backend\app\database.py`
- Create: `F:\AIOS\backend\app\errors.py`

- [ ] **Step 1: Create requirements.txt**

```
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
openai==1.58.1
pgvector==0.3.6
httpx==0.28.1
python-dotenv==1.0.1

# dev
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.22.0
```

- [ ] **Step 2: Install dependencies**

Run: `pip install -r F:\AIOS\backend\requirements.txt`

- [ ] **Step 3: Create pytest.ini**

```ini
[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = .
```

- [ ] **Step 4: Create app/__init__.py**

```python
# AIOS backend application package
```

- [ ] **Step 5: Create app/config.py**

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    database_url: str = "postgresql+asyncpg://aios:aios_dev@localhost:5432/aios"
    database_url_test: str = "postgresql+asyncpg://aios_test:aios_test@localhost:5433/aios_test"
    openai_api_key: str = ""
    openai_model: str = "gpt-4o-mini"

    class Config:
        env_file = ".env"
        extra = "ignore"


settings = Settings()
```

- [ ] **Step 6: Create app/database.py**

```python
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine
from sqlalchemy.orm import DeclarativeBase

from app.config import settings

engine = create_async_engine(settings.database_url, echo=False)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)


class Base(DeclarativeBase):
    pass


async def get_db() -> AsyncSession:
    async with async_session() as session:
        try:
            yield session
        finally:
            await session.close()
```

- [ ] **Step 7: Create app/errors.py**

```python
from fastapi import HTTPException, status


class NotFoundError(HTTPException):
    def __init__(self, resource: str, identifier: str):
        super().__init__(
            status_code=status.HTTP_404_NOT_FOUND,
            detail={"error": "not_found", "resource": resource, "id": identifier},
        )


class ConflictError(HTTPException):
    def __init__(self, field: str, value: str):
        super().__init__(
            status_code=status.HTTP_409_CONFLICT,
            detail={"error": "conflict", "field": field, "value": value},
        )
```

- [ ] **Step 8: Create app/main.py**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.api.router import router

app = FastAPI(title="AIOS", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(router, prefix="/api/v1")


@app.get("/")
async def root():
    return {"service": "AIOS", "version": "0.1.0"}
```

- [ ] **Step 9: Verify app scaffold loads**

Run: `cd F:\AIOS\backend && python -c "from app.main import app; print('OK')"`
Expected: Output shows `OK`.

---

### Task 3: Project Model + Alembic Migration

**Files:**
- Create: `F:\AIOS\backend\app\models\__init__.py`
- Create: `F:\AIOS\backend\app\models\project.py`

- [ ] **Step 1: Create app/models/__init__.py**

```python
from app.models.project import Project

__all__ = ["Project"]
```

- [ ] **Step 2: Create app/models/project.py**

```python
import uuid
from datetime import datetime

from sqlalchemy import Boolean, DateTime, String, Text, func
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column

from app.database import Base


class Project(Base):
    __tablename__ = "projects"

    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    name: Mapped[str] = mapped_column(String(255), unique=True, nullable=False)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )
    is_archived: Mapped[bool] = mapped_column(Boolean, default=False, server_default="false")
```

- [ ] **Step 3: Initialize and configure Alembic**

Run: `cd F:\AIOS\backend && alembic init alembic`

Update `alembic/env.py` target_metadata section:

```python
from app.database import Base
from app.models import Project  # noqa: F401

target_metadata = Base.metadata
```

Update `alembic/env.py` to use async run_migrations (replace the run_migrations_online function):

```python
import asyncio
from sqlalchemy.ext.asyncio import async_engine_from_config

def do_run_migrations(connection):
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()

async def run_async_migrations():
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()

def run_migrations_online():
    asyncio.run(run_async_migrations())
```

Also update `alembic/env.py` to set sqlalchemy.url from settings:

```python
from app.config import settings
config.set_main_option("sqlalchemy.url", settings.database_url.replace("+asyncpg", ""))
```

Update `alembic.ini` ? change `sqlalchemy.url` line to:
```
sqlalchemy.url = driver://user:pass@localhost/dbname
```

- [ ] **Step 4: Generate and run migration**

Run: `cd F:\AIOS\backend && alembic revision --autogenerate -m "create projects"`
Run: `cd F:\AIOS\backend && alembic upgrade head`

- [ ] **Step 5: Verify table**

Run: `docker compose exec db psql -U aios -d aios -c "\d projects"`
Expected: Shows columns id, name, description, created_at, updated_at, is_archived.

---

### Task 4: Project Schemas (Pydantic)

**Files:**
- Create: `F:\AIOS\backend\app\schemas\__init__.py`
- Create: `F:\AIOS\backend\app\schemas\project.py`

- [ ] **Step 1: Create app/schemas/__init__.py**

```python
from app.schemas.project import ProjectCreate, ProjectListResponse, ProjectResponse, ProjectUpdate

__all__ = ["ProjectCreate", "ProjectListResponse", "ProjectResponse", "ProjectUpdate"]
```

- [ ] **Step 2: Create app/schemas/project.py**

```python
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field


class ProjectCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=255)
    description: str | None = None


class ProjectUpdate(BaseModel):
    name: str | None = Field(None, min_length=1, max_length=255)
    description: str | None = None


class ProjectResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    name: str
    description: str | None
    created_at: datetime
    updated_at: datetime
    is_archived: bool


class ProjectListResponse(BaseModel):
    projects: list[ProjectResponse]
    total: int
```

- [ ] **Step 3: Verify schemas import**

Run: `cd F:\AIOS\backend && python -c "from app.schemas import ProjectCreate; print(ProjectCreate(name='test'))"`
Expected: `name='test' description=None`

---

### Task 5: Health Endpoint + Test Framework (TDD)

**Files:**
- Create: `F:\AIOS\backend\app\api\__init__.py`
- Create: `F:\AIOS\backend\app\api\router.py`
- Create: `F:\AIOS\backend\app\api\health.py`
- Create: `F:\AIOS\backend\tests\__init__.py`
- Create: `F:\AIOS\backend\tests\conftest.py`
- Create: `F:\AIOS\backend\tests\api\__init__.py`
- Create: `F:\AIOS\backend\tests\api\test_health.py`

- [ ] **Step 1: Write failing test**

Create `tests/api/test_health.py`:

```python
import pytest
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_health_returns_ok(client: AsyncClient):
    response = await client.get("/api/v1/health")
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "ok"
    assert "database" in data
```

- [ ] **Step 2: Create conftest.py**

Create `tests/conftest.py`:

```python
import pytest
from httpx import ASGITransport, AsyncClient

from app.main import app


@pytest.fixture
async def client():
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
```

- [ ] **Step 3: Run to confirm failure**

Run: `cd F:\AIOS\backend && pytest tests/api/test_health.py -v`
Expected: FAIL (404 endpoint not found).

- [ ] **Step 4: Create app/api/__init__.py**

```python
# API package
```

- [ ] **Step 5: Create app/api/router.py**

```python
from fastapi import APIRouter

from app.api.health import router as health_router

router = APIRouter()
router.include_router(health_router)
```

- [ ] **Step 6: Create app/api/health.py**

```python
from fastapi import APIRouter

from app.database import async_session

router = APIRouter()


@router.get("/health")
async def health():
    try:
        async with async_session() as session:
            await session.execute("SELECT 1")
        db_status = "connected"
    except Exception:
        db_status = "disconnected"

    return {"status": "ok", "database": db_status}
```

- [ ] **Step 7: Run to confirm pass**

Run: `cd F:\AIOS\backend && pytest tests/api/test_health.py -v`
Expected: PASS.

---



### Task 6: Project CRUD API (TDD)

**Files:**
- Create: `F:\AIOS\backend\app\api\projects.py`
- Create: `F:\AIOS\backend\tests\api\test_projects.py`
- Modify: `F:\AIOS\backend\app\api\router.py`

- [ ] **Step 1: Write failing tests**

Create `tests/api/test_projects.py`:

```python
import pytest
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_create_project(client: AsyncClient):
    response = await client.post("/api/v1/projects", json={
        "name": "My Project",
        "description": "A test project",
    })
    assert response.status_code == 201
    data = response.json()
    assert data["name"] == "My Project"
    assert data["description"] == "A test project"
    assert data["is_archived"] is False
    assert "id" in data


@pytest.mark.asyncio
async def test_list_projects(client: AsyncClient):
    await client.post("/api/v1/projects", json={"name": "Project A"})
    await client.post("/api/v1/projects", json={"name": "Project B"})

    response = await client.get("/api/v1/projects")
    assert response.status_code == 200
    data = response.json()
    assert data["total"] >= 2
    assert len(data["projects"]) >= 2


@pytest.mark.asyncio
async def test_list_projects_excludes_archived(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "Hidden"})
    pid = resp.json()["id"]
    await client.post(f"/api/v1/projects/{pid}/archive")

    response = await client.get("/api/v1/projects")
    data = response.json()
    names = [p["name"] for p in data["projects"]]
    assert "Hidden" not in names


@pytest.mark.asyncio
async def test_get_project_by_id(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "Target"})
    pid = resp.json()["id"]

    response = await client.get(f"/api/v1/projects/{pid}")
    assert response.status_code == 200
    assert response.json()["name"] == "Target"


@pytest.mark.asyncio
async def test_get_project_not_found(client: AsyncClient):
    response = await client.get("/api/v1/projects/00000000-0000-0000-0000-000000000000")
    assert response.status_code == 404


@pytest.mark.asyncio
async def test_update_project(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "Old Name"})
    pid = resp.json()["id"]

    response = await client.patch(f"/api/v1/projects/{pid}", json={"name": "New Name"})
    assert response.status_code == 200
    assert response.json()["name"] == "New Name"


@pytest.mark.asyncio
async def test_create_project_duplicate_name(client: AsyncClient):
    await client.post("/api/v1/projects", json={"name": "Duplicate"})
    response = await client.post("/api/v1/projects", json={"name": "Duplicate"})
    assert response.status_code == 409
```

- [ ] **Step 2: Run to confirm all fail**

Run: `cd F:\AIOS\backend && pytest tests/api/test_projects.py -v`
Expected: All FAIL (404).

- [ ] **Step 3: Implement CRUD endpoints**

Create `app/api/projects.py`:

```python
import uuid

from fastapi import APIRouter, Depends, Query
from sqlalchemy import select, func
from sqlalchemy.ext.asyncio import AsyncSession

from app.database import get_db
from app.errors import ConflictError, NotFoundError
from app.models.project import Project
from app.schemas.project import (
    ProjectCreate,
    ProjectListResponse,
    ProjectResponse,
    ProjectUpdate,
)

router = APIRouter(prefix="/projects", tags=["projects"])


@router.post("", response_model=ProjectResponse, status_code=201)
async def create_project(body: ProjectCreate, db: AsyncSession = Depends(get_db)):
    existing = await db.execute(select(Project).where(Project.name == body.name))
    if existing.scalar_one_or_none():
        raise ConflictError(field="name", value=body.name)

    project = Project(name=body.name, description=body.description)
    db.add(project)
    await db.commit()
    await db.refresh(project)
    return project


@router.get("", response_model=ProjectListResponse)
async def list_projects(
    include_archived: bool = Query(False),
    db: AsyncSession = Depends(get_db),
):
    query = select(Project)
    count_query = select(func.count()).select_from(Project)
    if not include_archived:
        query = query.where(Project.is_archived == False)  # noqa: E712
        count_query = count_query.where(Project.is_archived == False)  # noqa: E712
    query = query.order_by(Project.updated_at.desc())

    result = await db.execute(query)
    projects = result.scalars().all()

    total_result = await db.execute(count_query)
    total = total_result.scalar()

    return ProjectListResponse(
        projects=[ProjectResponse.model_validate(p) for p in projects],
        total=total,
    )


@router.get("/{project_id}", response_model=ProjectResponse)
async def get_project(project_id: uuid.UUID, db: AsyncSession = Depends(get_db)):
    result = await db.execute(select(Project).where(Project.id == project_id))
    project = result.scalar_one_or_none()
    if not project:
        raise NotFoundError(resource="project", identifier=str(project_id))
    return project


@router.patch("/{project_id}", response_model=ProjectResponse)
async def update_project(
    project_id: uuid.UUID,
    body: ProjectUpdate,
    db: AsyncSession = Depends(get_db),
):
    result = await db.execute(select(Project).where(Project.id == project_id))
    project = result.scalar_one_or_none()
    if not project:
        raise NotFoundError(resource="project", identifier=str(project_id))

    if body.name is not None:
        existing = await db.execute(
            select(Project).where(Project.name == body.name, Project.id != project_id)
        )
        if existing.scalar_one_or_none():
            raise ConflictError(field="name", value=body.name)
        project.name = body.name
    if body.description is not None:
        project.description = body.description

    await db.commit()
    await db.refresh(project)
    return project
```

- [ ] **Step 4: Update router.py**

```python
from fastapi import APIRouter

from app.api.agents import router as agents_router
from app.api.health import router as health_router
from app.api.projects import router as projects_router

router = APIRouter()
router.include_router(health_router)
router.include_router(projects_router)
router.include_router(agents_router)
```

- [ ] **Step 5: Run to confirm tests pass**

Run: `cd F:\AIOS\backend && pytest tests/api/test_projects.py -v`
Expected: All PASS.

Note: `test_list_projects_excludes_archived` and `test_get_project_not_found` may still fail if archive endpoint is not yet implemented (done in Task 7).

---

### Task 7: Archive/Restore API (TDD)

**Files:**
- Modify: `F:\AIOS\backend\app\api\projects.py`
- Modify: `F:\AIOS\backend\tests\api\test_projects.py`

- [ ] **Step 1: Write failing archive/restore tests**

Append to `tests/api/test_projects.py`:

```python
@pytest.mark.asyncio
async def test_archive_project(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "To Archive"})
    pid = resp.json()["id"]

    response = await client.post(f"/api/v1/projects/{pid}/archive")
    assert response.status_code == 200
    assert response.json()["is_archived"] is True


@pytest.mark.asyncio
async def test_restore_project(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "To Restore"})
    pid = resp.json()["id"]
    await client.post(f"/api/v1/projects/{pid}/archive")

    response = await client.post(f"/api/v1/projects/{pid}/restore")
    assert response.status_code == 200
    assert response.json()["is_archived"] is False


@pytest.mark.asyncio
async def test_list_includes_archived_when_requested(client: AsyncClient):
    resp = await client.post("/api/v1/projects", json={"name": "Show Archived"})
    pid = resp.json()["id"]
    await client.post(f"/api/v1/projects/{pid}/archive")

    response = await client.get("/api/v1/projects?include_archived=true")
    data = response.json()
    names = [p["name"] for p in data["projects"]]
    assert "Show Archived" in names


@pytest.mark.asyncio
async def test_archive_nonexistent_project(client: AsyncClient):
    response = await client.post("/api/v1/projects/00000000-0000-0000-0000-000000000000/archive")
    assert response.status_code == 404
```

- [ ] **Step 2: Run to confirm failure**

Run: `cd F:\AIOS\backend && pytest tests/api/test_projects.py -k "archive or restore" -v`
Expected: FAIL (404).

- [ ] **Step 3: Implement archive/restore**

Append to `app/api/projects.py`:

```python
@router.post("/{project_id}/archive", response_model=ProjectResponse)
async def archive_project(project_id: uuid.UUID, db: AsyncSession = Depends(get_db)):
    result = await db.execute(select(Project).where(Project.id == project_id))
    project = result.scalar_one_or_none()
    if not project:
        raise NotFoundError(resource="project", identifier=str(project_id))
    project.is_archived = True
    await db.commit()
    await db.refresh(project)
    return project


@router.post("/{project_id}/restore", response_model=ProjectResponse)
async def restore_project(project_id: uuid.UUID, db: AsyncSession = Depends(get_db)):
    result = await db.execute(select(Project).where(Project.id == project_id))
    project = result.scalar_one_or_none()
    if not project:
        raise NotFoundError(resource="project", identifier=str(project_id))
    project.is_archived = False
    await db.commit()
    await db.refresh(project)
    return project
```

- [ ] **Step 4: Run all project tests**

Run: `cd F:\AIOS\backend && pytest tests/api/test_projects.py -v`
Expected: All 11 tests PASS.

---

### Task 8: Agent + Change Model + Registry + Stub

**Files:**
- Create: `F:\AIOS\backend\app\agent\__init__.py`
- Create: `F:\AIOS\backend\app\agent\base.py`
- Create: `F:\AIOS\backend\app\agent\models.py`
- Create: `F:\AIOS\backend\app\agent\registry.py`
- Create: `F:\AIOS\backend\app\agent\plugins\__init__.py`
- Create: `F:\AIOS\backend\app\agent\plugins\codex_stub\__init__.py`
- Create: `F:\AIOS\backend\app\agent\plugins\codex_stub\plugin.py`
- Create: `F:\AIOS\backend\tests\agent\__init__.py`
- Create: `F:\AIOS\backend\tests\agent\test_registry.py`

- [ ] **Step 1: Write failing registry test**

Create `tests/agent/test_registry.py`:

```python
import pytest
from app.agent.registry import AgentRegistry


@pytest.mark.asyncio
async def test_registry_discovers_stub_agent():
    registry = AgentRegistry()
    await registry.discover()
    agents = await registry.list_agents()
    assert len(agents) >= 1
    names = [a.name for a in agents]
    assert "codex-stub" in names


@pytest.mark.asyncio
async def test_registry_find_by_capability():
    registry = AgentRegistry()
    await registry.discover()
    agents = await registry.find_by_capability("code_gen")
    assert len(agents) >= 1
    assert agents[0].name == "codex-stub"
```

- [ ] **Step 2: Run to confirm failure**

Run: `cd F:\AIOS\backend && pytest tests/agent/test_registry.py -v`
Expected: FAIL (module not found).

- [ ] **Step 3: Create Change model**

Create `app/agent/models.py`:

```python
from enum import Enum

from pydantic import BaseModel, Field


class ChangeAction(str, Enum):
    CREATE = "create"
    MODIFY = "modify"
    DELETE = "delete"


class ChangeStatus(str, Enum):
    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"
    APPLIED = "applied"
    FAILED = "failed"


class Change(BaseModel):
    path: str
    action: ChangeAction
    description: str
    content_before: str | None = None
    content_after: str | None = None
    diff: str | None = None
    status: ChangeStatus = ChangeStatus.PENDING
    error_message: str | None = None


class AgentResult(BaseModel):
    changes: list[Change] = Field(default_factory=list)
    summary: str = ""


class TaskInput(BaseModel):
    instruction: str
    context: dict = Field(default_factory=dict)
```

- [ ] **Step 4: Create BaseAgent**

Create `app/agent/base.py`:

```python
from abc import ABC, abstractmethod
from app.agent.models import AgentResult, TaskInput


class BaseAgent(ABC):
    name: str
    description: str
    capabilities: list[str]
    requires_confirmation: bool = True

    @abstractmethod
    async def execute(self, task: TaskInput) -> AgentResult:
        ...


class AgentError(Exception):
    def __init__(self, agent_name: str, message: str):
        self.agent_name = agent_name
        self.message = message
        super().__init__(f"[{agent_name}] {message}")
```

- [ ] **Step 5: Create AgentRegistry**

Create `app/agent/registry.py`:

```python
import importlib
import os
from pathlib import Path
from app.agent.base import BaseAgent


class AgentRegistry:
    def __init__(self):
        self._agents: dict[str, BaseAgent] = {}
        self._plugins_dir = Path(__file__).parent / "plugins"

    async def discover(self) -> None:
        self._agents.clear()
        if not self._plugins_dir.exists():
            return
        for entry in os.listdir(self._plugins_dir):
            plugin_path = self._plugins_dir / entry
            if not plugin_path.is_dir():
                continue
            init_file = plugin_path / "__init__.py"
            plugin_file = plugin_path / "plugin.py"
            if not init_file.exists() or not plugin_file.exists():
                continue
            module_path = f"app.agent.plugins.{entry}.plugin"
            module = importlib.import_module(module_path)
            if hasattr(module, "agent") and isinstance(module.agent, BaseAgent):
                self._agents[module.agent.name] = module.agent

    async def list_agents(self) -> list[BaseAgent]:
        return list(self._agents.values())

    async def find_by_capability(self, capability: str) -> list[BaseAgent]:
        return [a for a in self._agents.values() if capability in a.capabilities]
```

- [ ] **Step 6: Create stub agent**

Create `app/agent/plugins/__init__.py` (empty package marker).

Create `app/agent/plugins/codex_stub/__init__.py` (empty package marker).

Create `app/agent/plugins/codex_stub/plugin.py`:

```python
from app.agent.base import BaseAgent
from app.agent.models import AgentResult, TaskInput


class CodexStubAgent(BaseAgent):
    name = "codex-stub"
    description = "Stub agent representing Codex for P0 validation."
    capabilities = ["code_gen", "code_review", "refactor"]
    requires_confirmation = True

    async def execute(self, task: TaskInput) -> AgentResult:
        return AgentResult(
            summary=f"Stub: would process '{task.instruction[:50]}...'"
        )


agent = CodexStubAgent()
```

- [ ] **Step 7: Create app/agent/__init__.py**

```python
from app.agent.base import BaseAgent
from app.agent.models import AgentResult, Change, ChangeAction, ChangeStatus, TaskInput
from app.agent.registry import AgentRegistry

__all__ = [
    "BaseAgent",
    "AgentResult",
    "Change",
    "ChangeAction",
    "ChangeStatus",
    "TaskInput",
    "AgentRegistry",
]
```

- [ ] **Step 8: Run registry tests**

Run: `cd F:\AIOS\backend && pytest tests/agent/test_registry.py -v`
Expected: Both tests PASS.

---



### Task 9: Agent List Endpoint (TDD)

**Files:**
- Create: `F:\AIOS\backend\app\api\agents.py`
- Create: `F:\AIOS\backend\tests\api\test_agents.py`

- [ ] **Step 1: Write failing test** ? `tests/api/test_agents.py`

```python
import pytest
from httpx import AsyncClient

@pytest.mark.asyncio
async def test_list_agents(client: AsyncClient):
    response = await client.get("/api/v1/agents")
    assert response.status_code == 200
    data = response.json()
    assert "agents" in data
    names = [a["name"] for a in data["agents"]]
    assert "codex-stub" in names
```

- [ ] **Step 2: Run to confirm failure** ? `pytest tests/api/test_agents.py -v`
- [ ] **Step 3: Create `app/api/agents.py`** ? GET /agents reads from AgentRegistry
- [ ] **Step 4: Run to confirm pass**

---

### Task 10: LLM Abstractions + OpenAI Provider

**Files:**
- Create: `F:\AIOS\backend\app\llm\__init__.py`
- Create: `F:\AIOS\backend\app\llm\base.py`
- Create: `F:\AIOS\backend\app\llm\openai_provider.py`
- Create: `F:\AIOS\backend\tests\llm\__init__.py`
- Create: `F:\AIOS\backend\tests\llm\test_openai_provider.py`

- [ ] **Step 1: Write failing test** ? mock OpenAI client, test `chat()` and `chat_stream()`
- [ ] **Step 2: Run to confirm failure**
- [ ] **Step 3: Create `llm/base.py`** ? BaseLLMProvider ABC with `chat()` and `chat_stream()`
- [ ] **Step 4: Create `llm/openai_provider.py`** ? OpenAIProvider using AsyncOpenAI
- [ ] **Step 5: Run to confirm pass**

See design spec section 7.2 for exact interface definitions.

---

### Task 11: Voice Abstractions (STT/TTS Streaming)

**Files:**
- Create: `F:\AIOS\backend\app\voice\__init__.py`
- Create: `F:\AIOS\backend\app\voice\base.py`

- [ ] **Step 1: Create `voice/base.py`** ? BaseSTTProvider and BaseTTSProvider with both batch and streaming methods

```python
class BaseSTTProvider(ABC):
    async def transcribe(self, audio: bytes, language: str = "zh") -> str: ...
    async def transcribe_stream(self, audio_stream: AsyncIterator[bytes], language: str = "zh") -> AsyncIterator[str]: ...

class BaseTTSProvider(ABC):
    async def synthesize(self, text: str, voice: str = "default") -> bytes: ...
    async def synthesize_stream(self, text_stream: AsyncIterator[str], voice: str = "default") -> AsyncIterator[bytes]: ...
```

- [ ] **Step 2: Verify import** ? `python -c "from app.voice import BaseSTTProvider, BaseTTSProvider"`

---

### Task 12: Frontend Scaffold (Vite + React + TypeScript)

**Files:** All under `F:\AIOS\frontend\`

- [ ] **Step 1: Create `package.json`** ? React 18.3, Vite 6, TypeScript 5.6, @vitejs/plugin-react
- [ ] **Step 2: Create `tsconfig.json`** ? strict, ESNext, JSX react-jsx
- [ ] **Step 3: Create `vite.config.ts`** ? proxy /api to localhost:8000
- [ ] **Step 4: Create `index.html`**
- [ ] **Step 5: Create `src/main.tsx`** ? React 18 createRoot
- [ ] **Step 6: Create `src/index.css`** ? dark theme CSS variables + reset
- [ ] **Step 7: Create `src/api/client.ts`** ? typed fetch wrapper with Project, Agent interfaces and all API methods
- [ ] **Step 8: `npm install && npx tsc --noEmit`** ? no errors

---

### Task 13: Frontend Projects Page

**Files:**
- Create: `F:\AIOS\frontend\src\pages\Projects.tsx`
- Modify: `F:\AIOS\frontend\src\App.tsx`

- [ ] **Step 1: Create `Projects.tsx`** ? full CRUD page: load projects from API, create form (name + desc), archive/restore buttons, error display, show-archived checkbox. Dark theme.
- [ ] **Step 2: Update `App.tsx`** ? render `<Projects />`
- [ ] **Step 3: `npx tsc --noEmit && npx vite build`** ? no errors, dist/ produced

---

### Task 14: Mobile Shell (Expo)

**Files:** All under `F:\AIOS\mobile\`

- [ ] **Step 1: Create `package.json`** ? expo ~52, react 18.3, react-native 0.76
- [ ] **Step 2: Create `app.json`** ? name "AIOS", dark theme, portrait
- [ ] **Step 3: Create `tsconfig.json`** ? extends expo/tsconfig.base
- [ ] **Step 4: Create `App.tsx`** ? dark background, "AIOS" title, subtitle
- [ ] **Step 5: `npm install && npx tsc --noEmit`** ? no errors

---

### Task 15: Integration Test

- [ ] **Step 1: Start DB** ? `docker compose up -d db`
- [ ] **Step 2: Migrate** ? `cd backend && alembic upgrade head`
- [ ] **Step 3: Run all tests** ? `cd backend && pytest -v --cov=app --cov-report=term-missing`
Expected: All tests pass.
- [ ] **Step 4: Start server** ? `cd backend && uvicorn app.main:app --host 127.0.0.1 --port 8000`
- [ ] **Step 5: Verify health** ? `curl http://localhost:8000/api/v1/health`
- [ ] **Step 6: Verify agents** ? `curl http://localhost:8000/api/v1/agents`
- [ ] **Step 7: Verify CRUD flow** ? create, list, archive, restore, include_archived via curl
- [ ] **Step 8: Start frontend** ? `cd frontend && npx vite --port 5173`
- [ ] **Step 9: Verify mobile** ? `cd mobile && npx expo start`

---

## Completion Criteria

- [ ] `docker compose up -d db` ? PostgreSQL + pgvector on port 5432
- [ ] `pytest -v` in backend/ ? all tests pass
- [ ] `alembic upgrade head` ? projects table created
- [ ] `GET /api/v1/agents` ? returns codex-stub
- [ ] Archive ? Restore ? full cycle works
- [ ] `GET /projects?include_archived=true` ? shows archived
- [ ] Frontend `vite` on 5173 with proxy to backend
- [ ] Mobile App.tsx shows AIOS placeholder
- [ ] TypeScript compiles clean in frontend/ and mobile/
